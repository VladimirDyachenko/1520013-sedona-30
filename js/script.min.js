const form=document.querySelector(".find-form"),searchButton=document.querySelector(".call-to-action__button");form.classList.add("hidden"),searchButton.addEventListener("click",()=>{form.classList.toggle("hidden")});
